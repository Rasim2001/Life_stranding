using UnityEngine;

namespace WeatherSystem
{
    // Якорь для эффектов, которые должны визуально окружать обзор игрока. Партиклы —
    // дети этого объекта на камере, а не внутри зоны: зона на входе/выходе только
    // включает/выключает их. Если бы партиклы стояли неподвижно в зоне, паук прошёл бы
    // сквозь них за несколько метров и остался бы в зоне уже без эффекта вокруг себя.
    public class CameraFXHost : MonoBehaviour
    {
    }
}
